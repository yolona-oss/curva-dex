export enum managerStatus {
    offline = 0,
    online = 1,
    inChat = 2,
};

export enum chatStatus {
    closed = 0,
    pending = 1,
    active = 2,
};

export enum messageStatus {
    handled = 0,
    unHandled = 1,
};
